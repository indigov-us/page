{
  "name": "constituent-channel-messaging-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "flow": "flow",
    "check": "yarn run flow && yarn run standard",
    "prestart": "flow-remove-types src/ -d dist/",
    "start": "node dist/index.js",
    "start-local": "DB_NAME=pnights DB_HOST=localhost DB_PASSWORD=password DB_USER=user yarn start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.3.0",
    "mysql2": "^1.4.2",
    "sequelize": "^4.22.5"
  },
  "devDependencies": {
    "eslint-plugin-flowtype": "^2.39.1",
    "flow-bin": "^0.58.0",
    "flow-remove-types": "^1.2.3",
    "standard": "^10.0.3"
  },
  "standard": {
    "ignore": [
      "flow-typed"
    ],
    "plugins": [
      "flowtype"
    ]
  }
}
